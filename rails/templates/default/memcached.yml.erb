<% (['development', 'production'] + [@environment]).uniq.each do |env| -%>
<%= env %>:
  servers:
    <% @memcached[:hosts].each do |host| %>
    - <%= host[:host] || 'localhost' %>:<%= host[:port] %>
    <% end %>
<% end -%>
